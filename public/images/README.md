# BARBER HUB image slots

Put `.jpg`, `.png`, or `.webp` files in this folder and set `imageUrl` in `lib/mockData.ts`.

Example:

```ts
imageUrl: "/images/editor-pick-fade.jpg"
```

If the image file does not exist yet, BARBER HUB automatically shows the premium gray placeholder.
