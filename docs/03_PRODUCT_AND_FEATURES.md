# Product and Features

この文書は、BARBER HUBの機能を「実装済み」「進行中 / 検証中」「将来構想」に分けて整理します。

## 実装済みの主要機能

| 機能 | 状態 | 概要 |
| --- | --- | --- |
| 会員登録 / ログイン | 実装済み | Supabase Authを使用 |
| プロフィール | 実装済み | `profiles` とプロフィール編集、プロフィール画像を扱う |
| Snap | 実装済み | 投稿、一覧、詳細、画像アップロード、Thanks、コメント、保存系 |
| 記事 | 実装済み | 記事投稿、最大4枚画像、対象カテゴリのYouTubeリンク、記事表示、リアクション、コメント系 |
| アプリ内通知 | 実装済み | Snap/記事のThanks、いいね、コメント、Snapコメントいいね、フォローを本人向けに通知 |
| Q&A | 実装済み | 質問、回答、一覧、詳細 |
| Back Room | 実装済み | 会員限定コミュニティ。Back Roomプロフィールが必要 |
| 求人 | 実装済み | 求人投稿、一覧、詳細、応募導線 |
| 事業承継 | 実装済み | 承継投稿、一覧、詳細、相談導線 |
| 広告問い合わせ | 実装済み | 広告掲載・問い合わせ系の送信処理 |
| お問い合わせ | 実装済み | サポート問い合わせ系 |
| 店舗ディレクトリ | 実装済み | 店舗検索、地域検索、認証済み絞り込み、詳細、認証申請 |
| ルール / 規約ページ | 実装済み | 利用規約、プライバシー、投稿ガイドライン等 |
| 講習会・予定 | 実装済み / 一部モック中心 | `/seminars` やプロフィール予定表示がある。永続的な講習会管理は要確認 |
| メモ | 実装途中 | マイページに「メモ機能は準備中」表示がある |
| 管理者画面 | 一部実装 | `/admin/barber-shops/import`, `/admin/barber-shops/claims` は非公開admin系領域として実装済み |
| CSV店舗取込管理 | 一部実装 | Barber Directory向けCSVのプレビュー、重複、エラー確認、取込履歴、バッチ管理 |
| ニュースキュレーション | 第2段階実装 | RSS収集、関連性判定、AI下書き、非公開確認画面、承認済みニュースの公開表示 |

## 認証 / 会員登録

会員登録はSupabase Authを使い、ユーザーにメールアドレスとパスワードを1回入力してもらった後、確認メール経由でメール確認を完了します。

守るべき仕様:

- 登録直後は「確認メールを送信しました」と表示し、確認前にログイン入力へ誘導しない。
- 確認メールのリンクは `/auth/callback` で受け、Supabaseの `code` をセッションへ交換する。Supabaseメールテンプレートが `token_hash` 型の場合も同じcallbackで扱えるようにする。
- callbackでセッション交換に成功した場合は、直接ログインページや保護ページへ飛ばさず、専用のメール確認完了画面を表示する。
- 確認リンクが使用済み、期限切れ、二重クリック、またはパラメータ不足で処理できない場合も、エラー表示前に既存の有効セッションを確認する。既存セッションがあれば成功扱いで確認完了画面へ進める。
- メール確認完了画面では「メール確認が完了しました」と表示し、「BARBER HUBを開く」ボタンを1回押してもらう。
- ボタン押下時にServer Actionでログイン状態を再確認し、正常なら指定された安全な `next`、未指定ならトップ画面へ移動する。ボタンはpending中に「確認しています...」を表示し、disabledにして二重送信を防ぐ。
- callback失敗かつ有効セッションがない場合は、成功時と同じ「BARBER HUBを開く」文言を使わず、`/login?next=...` へ案内する。ログイン画面へ渡す `next` は `safeNextPath` 済みの値だけを使う。
- 正常なセッションがある場合は、メールアドレスとパスワードを再入力させない。
- メールアプリ内ブラウザなど別ブラウザで開いたためセッションが確認できない場合は、成功・失敗が分かる専用画面で案内し、会員登録のやり直しへ誘導しない。
- `next` はアプリ内の相対パスだけを許可し、外部URLや `//` から始まるURLへリダイレクトしない。

## 会員登録後の利用導線

- 会員登録時点では全員が個人アカウントとして始まる。
- Snap、記事、Back Room、Q&Aの利用に店舗登録は必要ない。
- プロフィールの登録区分は本人の職種や立場を表す表示用情報であり、店舗管理権限や求人掲載権限とは分けて扱う。
- 求人、事業承継、居抜き、備品売却、店舗情報管理などの店舗機能が必要なユーザーだけ、登録後にマイページからサロン機能を追加する。
- サロン機能は、店舗検索から既存店舗の管理申請、または見つからない場合の新規店舗登録申請へ進む。
- 店舗と個人アカウントが正式に紐づき、`barber_shops.verification_status = 'verified'` になった場合だけ、求人掲載や開業・承継掲載などの店舗機能を利用できる。
- プロフィールで「サロンオーナー」等を選んだだけでは、店舗管理権限や求人掲載権限を与えない。

## 店舗ディレクトリ

現在の店舗ディレクトリは、理容店舗の検索・閲覧・認証申請を扱います。

実装済みの範囲:

- 掲載店舗数の取得
- 店名、都道府県、市区町村、住所、電話番号の部分一致検索
- 都道府県・市区町村による地域検索
- 認証済み店舗のみの絞り込み
- 店舗詳細ページ
- 未認証店舗の「この店舗のオーナーですか？」導線
- ログインユーザーによる店舗登録申請
- 既存店舗への認証申請
- 管理者による店舗管理申請の一覧、承認、却下
- 認証済みオーナーによる自店舗情報編集

守るべき仕様:

- 一般ユーザーが自分以外の店舗を編集できない。
- 一般ユーザーが `verification_status` を `verified` に変更できない。
- 認証申請は即時verifiedにしない。
- 申請中店舗はマイページに「確認中」として表示し、認証済み店舗として扱わない。
- 店舗管理申請フォームでは、店舗のオーナー、代表者、または店舗情報を管理する正当な権限があることをチェック必須で確認する。
- 店舗管理申請の審査は管理者専用の `/admin/barber-shops/claims` で行い、一般ユーザーには入口を出さない。
- 口コミ、星評価、ランキングは実装しない。
- CSV取込管理画面は管理者専用の `/admin/barber-shops/import` で扱う。一般ユーザーには入口を出さない。
- メーカー、ディーラー、学校、組合の画面やデータを店舗PRへ混ぜない。
- 店舗カードは写真なしでも成立する小型1カラムを基本にする。
- 大量店舗を無限に全件表示せず、検索・地域絞り込み・ページネーションを使う。
- トップでは大量カードではなく、掲載店舗数と検索入口を見せる。

## Back Room

Back Roomは、理美容業界向けの会員限定コミュニティです。

実装済みの範囲:

- Back Roomプロフィール作成
- 会員向け投稿
- 投稿詳細
- コメント
- スレッド作成者本人による詳細画面からのスレッド削除。削除前に本文・コメント・添付画像を元に戻せないことを確認し、処理中はボタンを無効化する。
- スレッドへの画像最大1枚添付。スレッドは既存のタイトル・本文必須条件を維持し、画像だけでは投稿できない。
- コメントへの画像最大1枚添付。コメントは本文または画像のどちらかを必須とし、画像だけでも投稿できる。
- 投稿前プレビュー、選択解除、圧縮中・送信中の表示、二重送信防止。
- JPEG / PNG / WebPのブラウザ圧縮後画像をprivate Storageへ保存し、表示時だけ短時間signed URLを発行する。
- 投稿失敗時に今回作成した画像レコードとStorage objectを補償削除する。
- 未ログイン・未登録ユーザー向け案内

画像投稿の守るべき仕様:

- 元画像はブラウザ側で長辺1600px以内へ縮小し、EXIFを除去する。WebPを優先し、必要時はJPEGへfallbackする。
- 圧縮後の画像は1枚2MB以内とし、Server Actionでも容量、MIME type、JPEG / PNG / WebPの画像シグネチャ、画像寸法を再検証する。
- HEIC / HEIFはブラウザでデコード・変換できる場合だけ投稿し、変換できない場合はサーバーへ送信しない。
- スレッド画像は既存のスレッド作成後にStorage upload、画像テーブル保存、保存確認を行う。コメント画像はStorage upload成功後に、検証済みSECURITY DEFINER RPCでコメント行と画像行を同一トランザクションへ保存する。途中で失敗した場合は、成功投稿として公開されていない作成途中の行に限り今回作成したStorage object、画像行、親行を補償削除し、再送信できるエラーへ戻す。
- コメントのDB側でも、公開状態のコメントは本文の非空または画像行の存在を必須とする。通常のauthenticated INSERT / UPDATEで本文なしコメントを作成・空化できず、画像だけのコメントは本人とBack Room参加条件、Storage object、path、画像メタデータを検証するServer Action経由のRPCだけで確定する。
- スレッド削除は、Server ActionでSupabase Authの本人IDを取得し、`backroom_posts.user_id`との一致を再確認する。クライアントから送ったuserIdを権限判定に使わず、他人・未ログイン・存在しない・削除済みのスレッドは削除しない。
- 本人削除では`backroom_posts`の親行を物理削除し、`backroom_comments`、`backroom_thread_images`、`backroom_comment_images`を外部キーcascadeで削除する。対象pathを全件取得して親IDへの所属を検証し、DB削除成功後にだけprivate `backroom-images`の対象objectをserver-onlyで削除する。
- 削除前の画像metadata取得では、`42P01`または`PGRST205`と対象の正しい画像テーブル名を両方確認できるmissing relationだけを画像0件として扱う。通信、認証、RLS、timeout、その他のDBエラーは削除を中止する。コメント0件では`backroom_comment_images`を問い合わせず、親行DELETEはowner条件付きexact countが1件のときだけ成功とする。
- Storage削除に失敗してもDB削除は巻き戻さず、スレッド削除成功として扱って詳細をサーバーログへ残す。記事とSnapの既存「削除依頼」フローは変更しない。
- 画像テーブルが未適用のPreviewでも、画像取得失敗だけでBack Room全体を停止しない。画像は非表示にし、テキストを表示する。
- Back Room一覧では代表画像を表示しない。カード高さを維持し、画像は詳細画面で大きく表示する。

要確認 / 将来検討:

- 管理者向けモデレーション画面
- 通報、非表示、審査ワークフロー
- 業界外ユーザーの扱い

## Snap

Snapは、画像付き投稿とリアクションを扱う機能です。

実装済みの範囲:

- 画像なし / 1枚 / 最大4枚の画像アップロード
- 投稿本文
- 新規Snap投稿ではカテゴリーをユーザーに選択させず、保存上必要な内部既定値として既存カテゴリーの「日常」を使う
- `/snap` 一覧上部では説明文とカテゴリータグ行を表示せず、SNAP見出しの直後にPICK「今日の一枚」を表示する
- PICKは従来の写真サイズ、カード幅、横スクロール、表示内容を維持する
- `/snap` のLATEST「新着Snap」は2列の専用コンパクトカードで表示し、写真は正方形にする
- 新着カードの複数画像はカード内で横スワイプでき、画像なしSnapも正方形のfallbackで表示する
- 新着カードの写真とキャプションは投稿詳細、投稿者アイコンと投稿者名はプロフィールへ遷移する
- 新着グリッドではThanks、いいね、コメント、保存、フォローの大型操作ボタンを表示せず、これらの機能は詳細画面など既存の表示場所で維持する
- Thanks
- コメント
- Snapコメントへのいいね
- 保存系
- 投稿前の安全確認

守るべき仕様:

- Snap画像は投稿前にブラウザ側で縮小・圧縮し、WebPを優先、必要時は圧縮JPEGへ変換する。
- 原寸画像や安全にデコードできないHEIC / HEIFはサーバーへ送信しない。
- 1投稿の圧縮後画像容量は最大4枚合計でおおむね4MB以内にする。
- 画像アップロードはサイズ、MIME type、ファイル名、圧縮後の画像シグネチャを検証する。
- 複数画像は選択順を表示順として扱う。
- 新しいSnap画像は `snap_images` を順序順に使い、既存の1枚Snapは `image_url` / `image_path` を1枚目としてフォールバック表示する。
- Snap表示では `storage_path` がある画像は現在のStorage URLを組み立てて表示し、空path、空URL、重複pathは表示配列に残さない。
- 自分の投稿へのThanksはポイント反映・公開競争に使わない。
- 投稿本文でHTMLやscriptを実行させない。
- 他人のSnapを編集・削除できないようRLSと操作条件を守る。
- `/snap` の新着専用カードをトップページ、共通Snapカード、Snap詳細、マイページ、プロフィールへ波及させない。
- 一覧表示の変更ではDB、RLS、Storage、Snap取得順、取得件数を変更しない。

## プロフィール登録区分

- プロフィール設定の通常選択肢には「理美容アシスタント」「サロンスタッフ」を表示しない。
- 「アシスタント」は通常選択肢として維持する。
- 既存プロフィールに保存済みの「理美容アシスタント」「サロンスタッフ」は一括変更せず、既存値として表示・保存互換を維持する。

## Thanks / いいね / 保存 / フォロー

- SnapのThanksは実装済み。
- Snapと記事の公開画面では、Thanks、いいね、コメント、保存を既存の永続化基盤で扱う。
- 記事のいいね、Thanks、保存は実装済み。
- 保存系、フォロー系は既存機能として扱う。
- 自分自身のThanks・いいねは保存・加算対象外にする。
- Snapと記事の公開画面ではThanks数・いいね数を表示しない。
- Snapと記事の公開画面ではコメント数を表示し、0件も「コメント 0」と表示する。
- Snapと記事のThanks追加操作は共通のキラキラ演出で即時フィードバックし、解除時には表示しない。
- 公開中かつ未削除のSnapコメントは全ユーザーが閲覧でき、投稿はログインユーザーに限定する。
- 公開中かつ未削除のSnapコメントには、ログインユーザーがコメント単位でいいねできる。自分のコメントには押せない。
- Snapコメントいいねはコメントごとの公開数と、自分が押しているかだけを画面に返す。誰が押したかの一覧は公開しない。
- Thanks数・いいね数は公開競争に使わず、投稿者本人だけがマイページで確認する。
- 本人専用のマイページ指標と、公開ランキング表現を混同しない。
- 求人、開業・承継にはSnap/記事用のThanks・いいね・コメント・保存UIを追加しない。

## 記事

記事は、理容師本人の経験、経営、技術、集客、AI活用、講習会・コンクールレポートなどを共有する機能です。

実装済みの範囲:

- 画像なし記事投稿。
- 最大4枚の記事画像投稿。
- 講習会・講習会レポート・コンクールレポートで任意のYouTubeリンク設定。
- 記事詳細、カテゴリー別一覧、マイページ、投稿者プロフィールでの記事表示。
- 記事へのThanks、いいね、コメント、保存。
- 運営者だけが記事投稿時にEDITOR'S PICK掲載指定できる。
- 運営者だけがマイページの記事管理からEDITOR'S PICK掲載を解除できる。

守るべき仕様:

- 記事画像は投稿前にブラウザ側で縮小・圧縮し、WebPを優先、必要時は圧縮JPEGへ変換する。
- 原寸画像や安全にデコードできないHEIC / HEIFはサーバーへ送信しない。
- 記事画像は圧縮後1枚あたり最大約2MBにする。
- 1記事の圧縮後画像容量は最大4枚合計でおおむね4MB以内にする。
- サーバー側でもMIME type、容量、JPEG / WebPの画像シグネチャを再検証する。
- 投稿失敗、DB保存失敗、保存確認失敗時は、今回アップロードした記事画像を削除する。
- 新規記事画像はprivate bucketへ保存し、公開中かつ未削除の記事表示時だけサーバー側で短時間のsigned URLを発行する。
- image_pathがなく既存のimage_urlだけを持つ記事は、互換性のため従来のURL表示を維持する。
- 記事カード、トップ、カテゴリー別一覧、マイページ、投稿者プロフィール、EDITOR'S PICKでは代表画像として1枚目だけを使い、カードサイズや余白を変えない。
- 記事詳細では本文中の `[[article-image:1]]` から `[[article-image:4]]` のマーカー位置に対応画像を表示できる。本文に挿入済みの画像はトップ位置で重複表示せず、未挿入画像またはマーカーなし画像は従来のトップ画像位置に選択順で表示する。
- 直接動画アップロードは実装しない。対象カテゴリのYouTube URLはサーバー側でYouTubeドメインと動画IDを検証し、記事詳細ではiframe埋め込みではなく軽量な外部リンクとして表示する。
- YouTube URLは任意。動画権利確認は、対象カテゴリでYouTube URLが実際に入力された場合だけ必須にする。
- 一般ユーザーがFormDataを改ざんしてもEDITOR'S PICKに設定できないよう、Server Action側で運営者判定を行う。
- `editor_pick_at` がないPreview環境では、記事投稿とトップ表示がクラッシュしないようにし、固定記事へはフォールバックしない。

カテゴリー対応:

| 表示導線 | 記事カテゴリー |
| --- | --- |
| 経営 | `経営`, `独立`, `求人`, `経験記事` |
| 集客 | `集客` |
| AI | `AI活用`, `AI` |
| 技術 | `技術`, `講習会`, `講習会レポート`, `コンクールレポート` |
| 道具 | `道具`, `メーカー新商品`, `店販`, `開業準備` |

記事メニューと関連記事:

- 記事は投稿カテゴリーに基づき、経営・集客・AI・技術・道具の各メニューへ自動表示する。手動記事IDの固定設定は使わない。
- 各メニューは公開中かつ未削除の記事を新しい順で表示し、実記事がある場所へ固定ダミーを混ぜない。
- 実記事が0件の場合は短い空状態、ほかのテーマ導線、新着記事導線を表示する。ログイン済みの場合だけ記事投稿ボタンを出す。
- 記事詳細の関連記事は、現在の記事を除外し、同一メニューを優先して最大3件表示する。不足時は定義済みの近接メニューまたは全体新着で補完する。
- 関連記事が同一メニューだけで成立する場合の見出しは「関連記事」、補完を使った場合は「あわせて読む」にする。
- 記事詳細および経営・集客・AI・技術・道具の各メニューに、関連Snapは表示しない。
- 記事詳細と各メニューには「ほかのテーマ」として経営、集客、AI、技術、道具の横断チップを1回だけ表示する。
- Back Room案内は補助導線として必要な場合だけ表示し、同一ページに繰り返さない。広告が表示されるページでは省略できる。

EDITOR'S PICK:

- 一般投稿者が自分で掲載を選ぶ仕様にしない。
- `BARBER_HUB_ADMIN_USER_IDS` を基本にし、互換用に `NEWS_REVIEW_ADMIN_USER_IDS` の運営者allowlistも利用できる。
- トップページでは `editor_pick_at` が設定された公開中・未削除記事を、最新の `editor_pick_at` 順で最大3件表示する。
- 運営選定記事が3件未満の場合は、固定記事ではなく公開中の実記事新着で不足分を補完する。実記事がない場合は短い空状態にする。
- 実投稿記事のカードでは、投稿画像、記事タイトル、投稿者名、投稿者アイコン、プロフィールリンク、記事詳細リンクを使う。
- カテゴリーに応じた短い英語タグを表示し、カードサイズは変えない。

広告枠:

- 広告表示は `content_ads` の掲載場所、対象メニュー、有効期間、有効状態、優先度に基づき、1ページ最大1件を表示する。
- 対象メニューは `all`, `management`, `marketing`, `ai`, `technique`, `tools` を使う。
- 掲載場所は `home`, `category_management`, `category_marketing`, `category_ai`, `category_technique`, `category_tools`, `article_bottom`, `backroom` を初期値として扱う。
- PR、広告、Sponsoredのいずれかを必ず明示し、外部リンクは `rel="sponsored noopener noreferrer"` を付ける。
- `image_path` がある広告画像はprivate `content-ad-images` bucketからサーバー側で短時間のsigned URLを発行して表示する。`image_path` がない既存広告だけ `image_url` を互換表示に使う。
- 広告がない場合は空枠、ダミー広告、広告募集中カードを一般画面へ常設しない。

## アプリ内通知

実装済みの範囲:

- 自分のSnapへのThanks、いいね、コメント。
- 自分のSnapコメントへのいいね。
- 自分の記事へのThanks、いいね、コメント。
- 自分への新規フォロー。
- 通知一覧、通知ごとの既読、すべて既読、Bottom Navigationの未読バッジ。

守るべき仕様:

- 通知はアプリ内通知のみ。Push通知、メール、SMS、Web Push、リアルタイム購読、通知設定画面はまだ実装しない。
- 自分自身の操作では通知を作らない。
- 通知文は操作したユーザーが分かる文章にする。
- 解除されたThanks、いいね、Snapコメントいいねの通知は残さない。
- フォロー成立時は「〇〇さんがあなたをフォローしました」を作成し、フォロー解除時は対応する通知を削除する。既存フォローのbackfillは行わない。
- 通知のactor画像・表示名はactorプロフィールへ、本文は対象Snap / 記事 / コメントへ遷移させ、フォロー通知本文はactorプロフィールへ遷移させる。
- マイページ本人専用の「フォロワー」一覧はフォローされた新しい順に表示し、各行から公開プロフィールへ遷移させる。取得失敗は空状態と区別して表示する。
- 削除または非公開になったSnap/記事/コメントの通知は一覧で壊れたリンクにしない。
- 未読バッジは本人の未確認情報として小さく表示し、ランキングや人気競争の表現にしない。

公開プロフィールには、今回もThanks総数、いいね総数、Thanksポイント、本人専用の反応統計、フォロワー人物一覧を追加しない。既存のフォロワー数・フォロー中数は維持する。

## 記事 / Q&A / 求人 / 承継

これらは既存機能として扱い、店舗ディレクトリやUI調整PRで壊さない。

- 記事カード、求人カード、写真カードの画像余白は、対象PRでない限り変更しない。
- 求人、承継、Q&Aの投稿導線や保存処理を、関係ないPRで触らない。
- 各投稿画面の注意書きやチェック欄を不用意に変更しない。

トップページQ&A表示:

- トップページは `qa_questions` から公開可能な質問を最新順で最大3件取得して表示する。
- Q&A詳細へ遷移できる質問タイトル、カテゴリ、解決状態、公開コメント数を表示する。
- 固定ダミーQ&Aや、実データ0件時のダミーfallbackは表示しない。
- 実データが0件の場合は、Q&A見出しと既存のQ&A投稿導線だけを残す。
- 取得エラー時もダミー質問へ戻さず、トップ画面全体を壊さない範囲でQ&Aカードなしにする。

トップページCONTRIBUTION:

- CONTRIBUTIONは「経験を未来に残そう」「繋がれば　もっと面白い」を上部文言として使う。
- Thanks、投稿の積み重ね、マイページでの本人確認、バッジ、編集部セレクト、将来特典へつながる説明に絞る。
- カード内の不要な下部補足文は削除し、説明量と高さを抑える。
- 「将来の特典」カードの見出し、本文、下部文言、アイコンは維持する。

トップページ自動スクロールカード:

- Q&A下、メーカー新商品上にあった黒い自動スクロール記事カードは削除済み。
- メーカー新商品セクション、講習会、学生・求人の横スクロール導線は維持する。

## 3MIN NEWS自動化

実装済みの範囲:

- 公式または信頼できるRSS / APIからニュースメタデータを取得する。
- 理容師、サロン経営、技術、衛生、人材、お客様との会話との関連性を判定する。
- 関連性が低いニュースはAI下書き生成の対象外にする。
- AIはBARBER HUB向けの下書きを作成する。
- AIは仕事・経営、理容師本人、お客様との会話の3視点でタイトル候補を作成する。
- 下書きは `pending` の非公開状態で保存する。
- 運営者だけが `/news-review` でタイトル候補を比較し、最終タイトルを選択・修正してから確認、承認、非掲載、保留を行う。
- 運営者が `approved` にしたニュースだけをトップページの3MIN NEWSとニュース詳細へ表示する。
- 公開ニュースが不足または取得失敗した場合は、既存の固定ニュースをフォールバック表示する。

守るべき仕様:

- ニュースを自動公開しない。
- タイトル候補を選んだだけで公開せず、公開タイトルは最終的な `draft_title` とする。
- `title_candidates` と `primary_angle` は内部編集用で、一般ユーザー向け公開RPCには返さない。
- トップページの3MIN NEWSは最大4件表示を維持する。
- `pending`, `rejected`, 生成エラーあり、重複候補、不完全な下書きは公開しない。
- 元記事本文全体を保存・転載しない。
- HTML本文の無断スクレイピングをしない。
- 情報源にない数値、人物発言、断定をAIに作らせない。
- 既存の3MIN NEWS固定表示と `mockData.ts` は削除せず、フォールバックとして残す。これは記事ダミー撤去の対象外であり、記事回遊用の固定記事・関連記事・広告fallbackと混同しない。
- トップページの3MIN NEWSは、管理者が承認して公開した日時である `reviewed_at` の降順で最大4件表示する。重要度、`risk_level`、WORK / STYLE / TALK分類、カテゴリー別件数制限はトップ4件の選定と並び順に使用しない。

2026年7月時点の拡張方針:

- 若い男性理容師が毎日見たくなる入口として、3MIN NEWSを仕事・提案・会話の3方向へ広げる。
- 内部分類は `work`, `style`, `talk` を使い、公開UIへ内部スコアや内部理由は出さない。
- `work` は理容師の仕事、技術、経営、制度、衛生、安全、人材、AI、店舗運営に直接関係する情報を扱う。
- `style` はメンズファッション、メンズヘア、ヘアケア、頭皮ケア、メンズスキンケア、香水、身だしなみなど、施術提案・店販・男性客との会話に使える情報を扱う。
- `talk` は音楽、店内BGM、映画・ドラマ、エンタメ、主要スポーツ、若い男性の生活トレンドなど、接客中の自然な会話や店内体験づくりに使える情報を扱う。
- 芸能・エンタメは作品、活動、イベント、ファッション、ヘア、公式発表を中心とし、不倫、離婚、恋愛ゴシップ、私生活暴露、SNS炎上だけの記事は対象外にする。
- スポーツは主要大会、日本代表、日本人選手の大きな話題など、多くのお客様との会話に使える範囲に限定し、スポーツ速報サイト化しない。
- 医療的な発毛効果、美容効果、治療効果を断定する候補はAI前に除外または高リスク扱いにし、公式情報確認を前提にする。
- 音楽や店内BGMでは歌詞・音源・動画を保存または転載しない。店舗BGM利用条件は公式情報確認を促す。
- トップページの3MIN NEWSは最大4件のまま維持し、公開可能な最新4件が同じWORK / STYLE / TALK分類に偏っていても、承認公開順の最新4件をそのまま表示する。
- WORK / STYLE / TALK分類や `risk_level` は収集、AI下書き、運営確認など内部判断には残すが、トップページの4件選定・並び順には使わない。
- 公開ニュースが不足する場合は、仕事、ヘア・美容、身だしなみ、会話ネタの固定fallbackで補完する。

2026年7月時点の追加運用:

- 有効取得元は、厚生労働省新着情報、IPA重要なセキュリティ情報、金融庁新着情報、総務省新着情報を基本とする。
- 流行系の追加取得元は、FASHIONSNAP、WWDJAPAN、音楽ナタリー、映画ナタリー、NHKニュース スポーツの正規RSSを使う。
- 取得元は `enabled`, `sourceGroup`, `priority`, `maxCandidatesPerRun` を持ち、問題がある取得元だけをコード上で停止できる。
- `sourceGroup` は `health_labor`, `business_money`, `digital_security`, `product_safety`, `fashion_beauty`, `music_entertainment`, `sports_talk` などを使い、1回の下書き生成で1分野だけに偏らないようにする。
- 1回のAI下書き候補は全取得元合計で最大6件とし、1分野あたり最大2件、同一取得元は原則最大2件までに抑える。
- AI下書き候補は、WORK最大3件、STYLE最大2件、TALK最大1件を目安にし、無制限に流行領域を増やさない。
- 候補化は新着順だけではなく、ルールベースの関連度、低品質行政告知の除外、AIによる3分価値判定を組み合わせる。
- AI判定では、理容師が朝・昼・夕方にBARBER HUBを開いて3分読む価値があるかを評価し、関連性、興味、緊急性、実用性、会話化しやすさを見ます。
- AIが使えない場合でも収集処理全体は止めず、ルールベース判定へfallbackする。
- 同一URL、正規化タイトル、同じ制度名・主要語句、直近7日以内の公開ニュースとの類似は候補から除外する。
- トップページの3MIN NEWSは4件表示のまま維持し、価値判断や分類バランスではなく、管理者が承認公開した `reviewed_at desc`, `updated_at desc`, `created_at desc`, `id desc` の順で最新4件を表示する。
- トップページでは、運営承認により公開状態になった `reviewed_at` から12時間以内の公開ニュースだけ、新聞アイコン上へ小さく `NEW` を表示する。

## CSV店舗取込管理

実装済みの範囲:

- 管理者専用CSV選択画面。
- CSV内容プレビュー。
- 必須項目チェック。
- 完全一致重複と類似候補の表示。
- 登録実行と登録結果表示。
- 取込バッチと取込行の履歴保存。

守るべき仕様:

- CSVデータ本体をmigrationやseedに含めない。
- 店名、都道府県、市区町村、掲載元は必須にする。
- 住所と電話番号は空欄でも取込可能にする。
- 電話番号はtextとして保存し、先頭0を落とさない。
- 内線付きなどの電話番号形式ゆれは取込前確認件数として表示し、行エラーだけで登録対象から外さない。
- 東京都CSVの市区町村列は検索・地域絞り込み用に正式自治体名へ正規化する。住所列は元資料の完全住所を維持し、市区町村列と混同しない。
- CSVの「未認証」は `unverified` として保存し、認証済みのように見せない。
- 久留米支部組合員名簿由来のデータを「久留米市全店舗」と断定しない。

## 公式一覧からCSVを作成

実装済みの小さな管理補助機能:

- 管理者専用の `/admin/barber-shops/import/source` で自治体・保健所の公式ページまたは掲載ファイルURLを取得する。
- CSV、TSV、xlsx、HTML table、テキスト抽出可能なPDFを、既存の7列CSV形式へ変換する。元データの列名を自動判定し、判定できない場合は店名・住所・電話番号の列を管理者が選択する。
- 都道府県、市区町村、掲載元名を管理者が入力でき、元データにない都道府県・市区町村は入力値だけで補完する。住所から自治体を推測して確定しない。
- 代表者氏名など公開に不要な個人情報は出力せず、郵便番号も現行CSV形式に列がないため出力しない。
- HTMLはGETしたページに店舗一覧テーブルが直接含まれる場合だけ解析する。検索フォームだけのページは500にせず、検索結果URLまたはCSV、Excel、PDFの指定を案内する。ページネーションを検出した場合は自動巡回せず、取得件数（このURL）とページ内表示件数を示して1ページ分だけ扱う。
- 掲載元名は管理者の入力値を保存し、組合サイトは「組合加盟店一覧」など、公的な全施設一覧と区別できる名称を入力する。組合加盟店一覧を地域の全施設一覧とは扱わない。
- 取得結果の件数、空欄、除外行、エラーを一時表示し、先頭20件程度を確認できる。生成CSVを管理者がダウンロードする。既存のCSV取込バッチは作成せず、ダウンロード後に `/admin/barber-shops/import` でCSVを選択し、既存のプレビュー、重複判定、管理者による登録実行を使う。
- 画像だけのPDF、OCR、CAPTCHA、ログイン必須ページ、JavaScript操作が必要な一覧、複雑な複数ページクロールは対象外とし、「このページまたはファイルは自動解析できませんでした。手動で確認してください」と案内する。
- CSVはUTF-8 BOMで出力し、Excel等で式として解釈されるセル先頭文字を安全化する。
- 外部URLは管理者だけが取得でき、HTTPS、DNS/IPの内部アドレス拒否、タイムアウト、取得容量、リダイレクト回数、対応MIME確認を行う。

## 将来構想

- 業界ディレクトリ拡張: メーカー、ディーラー、学校、組合など。
- CSV取込時のプレビュー、重複確認、エラー確認、取込履歴、取込バッチ単位の管理。
- 店舗データの追加・修正・閉店申請の運用画面。
- 公式認証、協賛、広告商品の拡張。
- ニュースや業界情報の運用効率化。
- 管理画面と審査ワークフロー。

## 追加しない方針

- 店舗の口コミ。
- 店舗の星評価。
- 店舗ランキング。
- 未承認の実在店舗情報の大量登録。
- 本番seedの自動投入。
- mainマージだけで即時本番DB変更される完全自動migration。

## PARTNERS問い合わせ

- トップ最下部のLegal Links直前にPARTNERSカードを置き、`/partners/contact`へ遷移させる。
- 既存の`/partners`はメーカー・ディーラー情報、パートナー導線、新商品・連動商品、掲載モデル、メーカー掲載問い合わせを扱う既存ページとして維持する。
- `/partners/contact`では、業種を限定せず、協賛・広告掲載・タイアップ・共同企画・その他の相談を内容に応じて個別に受け付ける。料金は掲載場所、期間、内容、制作の有無を確認したうえで個別案内する。
- 受付はサイト内フォームを正式な問い合わせ先とし、個人メールアドレスは直接公開しない。問い合わせ内容は非公開で、`/admin/partner-inquiries`から管理者だけが確認する。
- `content_ads`は一般利用者へ広告を表示する機能、PARTNERSカードとフォームは広告・協賛を検討する企業・団体の相談入口であり、役割を混同しない。

### プロフィール写真の変更・削除

- 本人はプロフィール編集画面から丸いアイコン写真とカバー写真を変更できる。
- 丸いアイコン写真とカバー写真は個別に削除できる。削除指定は保存時に確定し、保存前はDB・Storageを変更しない。
- 新しい写真の選択取り消しと、削除予定の取り消しができる。
- プロフィール変更はSupabase Authで確認した本人の`profiles.id = auth.uid()`に限る。
